$("#badges_req_block").html("<%= j render partial: 'badges/requests', :locals => {:user => current_user} %>");
$("#badges_block").html("<%= j render partial: 'badges/badges', :locals => {:user => current_user} %>");
$("#new_my_badge")[0].reset();
$("#badge_modal").modal('hide');